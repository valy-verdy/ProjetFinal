{% extends 'dashboardbase.html.twig' %}

{% block title %}Categorie
{% endblock %}

{% block body %}
	<a class="btn btn-primary" href="{{ path('app_category_index') }}">Retour</a>
	<h1>Catégorie</h1>
	<div class="container">
		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ category.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ category.name }}</td>
				</tr>
			</tbody>
		</table>

		<a class="btn btn-warning" href="{{ path('app_category_edit', {'id': category.id}) }}">Modifier</a>

		{{ include('category/_delete_form.html.twig') }}
	</div>
{% endblock %}
