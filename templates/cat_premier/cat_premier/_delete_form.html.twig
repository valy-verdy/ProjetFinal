<form method="post" action="{{ path('app_cat_premier_delete', {'id': cat_premier.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cat_premier.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
