{% extends 'dashboardbase.html.twig' %}

{% block title %}PhotosProduct
{% endblock %}

{% block body %}
	<h1>PhotosProduct</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ photos_product.id }}</td>
			</tr>
			<tr>
				<th>Link</th>
				<td>{{ photos_product.link }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_photos_product_index') }}">back to list</a>

	<a href="{{ path('app_photos_product_edit', {'id': photos_product.id}) }}">edit</a>

	{{ include('photos_product/_delete_form.html.twig') }}
{% endblock %}
