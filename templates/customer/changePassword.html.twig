{% extends 'base.html.twig' %}


{% block body %}
    <h1>{{ 'changer mot de passe'|trans }}</h1>

	<div class="container">

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> {{ 'Enregistrer'|trans }}
        </button>
    {{ form_end(form) }}
    </div>
{% endblock %}

